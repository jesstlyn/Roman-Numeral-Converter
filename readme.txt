This project is using vue.js and run in local host. 
It allows you to convert Roman to Numeral or vice versa. 

How to run the project: 
    1. In your terminal, Go to the project directory "Roman Numeral COnverter"
    2. In terminal, type "npm install" 
    3. then type "npm run dev" 
